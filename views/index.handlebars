<h1>Burgers!</h1>

<h2>Which burgers would you like to eat!</h2>


<ul>
    {{#each burgers}} {{#unless eaten}} {{> burger-block eaten=true}} {{/unless}} {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul>
    {{#each burgers}} {{#if eaten}} {{> burger-block eaten=false}} {{/if}} {{/each}}
</ul>

<h2>Please add a NEW Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burgers_tb">Burger Name:</label>
        <input type="text" id="BurgerName" name="name">
    </div>

    <div class="form-group">
        <label for="eaten">Has this burger been eaten?</label>
        <br>
        <input type="radio" name="eaten" value="1" checked> eaten!
        <br>
        <input type="radio" name="eaten" value="0"> list
    </div>

    <button type="submit">Add Burger to list</button>
</form>